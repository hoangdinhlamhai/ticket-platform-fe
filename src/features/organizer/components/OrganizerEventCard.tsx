import { OrganizerStatusBadge } from './OrganizerStatusBadge.tsx'
import type { OrganizerEvent } from '../types/organizer-event.ts'

type Props = { event: OrganizerEvent; onOpen: (eventId: string) => void; revenue: number }
const labels = { draft: 'Bản nháp', pending_review: 'Chờ duyệt', changes_requested: 'Cần chỉnh sửa', approved: 'Đã duyệt', published: 'Đã xuất bản', ongoing: 'Đang diễn ra', ended: 'Đã kết thúc', cancelled: 'Đã hủy', rejected: 'Từ chối' } as const
const tones = { draft: 'neutral', pending_review: 'blue', changes_requested: 'coral', approved: 'mint', published: 'mint', ongoing: 'blue', ended: 'neutral', cancelled: 'coral', rejected: 'coral' } as const
function date(value: string) { return new Intl.DateTimeFormat('vi-VN', { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(value)) }
export function OrganizerEventCard({ event, onOpen, revenue }: Props) { return <article className="rounded-lg border border-line bg-surface p-4"><div className="flex items-start justify-between gap-3"><h2 className="m-0 text-lg font-extrabold">{event.title}</h2><OrganizerStatusBadge label={labels[event.status]} tone={tones[event.status]} /></div><dl className="mt-4 grid grid-cols-2 gap-3 text-sm"><div><dt className="font-bold text-ink-soft">Thời gian</dt><dd className="m-0 mt-1">{date(event.startsAt)}</dd></div><div><dt className="font-bold text-ink-soft">Doanh thu</dt><dd className="m-0 mt-1">{revenue.toLocaleString('vi-VN')}đ</dd></div><div className="col-span-2"><dt className="font-bold text-ink-soft">Địa điểm</dt><dd className="m-0 mt-1">{event.venue}, {event.city}</dd></div></dl><button className="mt-4 min-h-11 w-full rounded-md border border-blue px-4 text-sm font-extrabold text-blue-deep" type="button" onClick={() => onOpen(event.id)}>Mở sự kiện</button></article> }
