import type { OrganizerOrderFilters } from '../helpers/filter-organizer-orders.ts'
import type { OrganizerPaymentStatus } from '../types/organizer-commerce.ts'
type Props = { filters: OrganizerOrderFilters; onChange: <K extends keyof OrganizerOrderFilters>(key: K, value: OrganizerOrderFilters[K]) => void; onReset: () => void }
export function OrganizerOrderFilters({ filters, onChange, onReset }: Props) { return <section className="grid gap-3 rounded-lg border border-line bg-surface p-4 sm:grid-cols-[minmax(0,1fr)_12rem_auto]" aria-label="Lọc đơn hàng"><label className="text-sm font-bold">Tìm đơn hàng<input className="mt-2 min-h-11 w-full rounded border border-line bg-paper px-3" placeholder="Mã đơn, người mua, email" value={filters.query} onChange={(event) => onChange('query', event.target.value)} /></label><label className="text-sm font-bold">Thanh toán<select className="mt-2 min-h-11 w-full rounded border border-line bg-paper px-3" value={filters.paymentStatus} onChange={(event) => onChange('paymentStatus', event.target.value as OrganizerPaymentStatus | 'all')}><option value="all">Tất cả</option><option value="paid">Đã thanh toán</option><option value="pending">Chờ thanh toán</option><option value="refunded">Đã hoàn tiền</option></select></label><button className="mt-auto min-h-11 rounded border border-blue px-3 font-bold text-blue-deep" type="button" onClick={onReset}>Đặt lại</button></section> }
