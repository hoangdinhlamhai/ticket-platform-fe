import { forwardRef } from 'react'
import { formatTicketPrice } from '../../events'
const cells = [0, 1, 2, 3, 5, 8, 9, 10, 12, 14, 17, 18, 20, 22, 24, 26, 27, 29, 31, 32, 34, 36, 38, 40, 41, 43, 45, 46, 48]
type Props = { acceptedTerms: boolean; amount: number; error?: string; transferContent: string; onAcceptedTermsChange: (value: boolean) => void }
export const PrimaryVietQrPanel = forwardRef<HTMLInputElement, Props>(function PrimaryVietQrPanel({ acceptedTerms, amount, error, transferContent, onAcceptedTermsChange }, ref) {
  return <section className="rounded-lg border border-line bg-paper-deep p-5"><p className="text-xs font-extrabold tracking-[0.1em] text-coral-dark">VIETQR MINH HỌA</p><div className="mt-4 grid grid-cols-[10rem_minmax(0,1fr)] gap-5 mobile:block"><div className="relative grid aspect-square grid-cols-7 gap-1 border-8 border-surface bg-surface p-2 mobile:mx-auto mobile:max-w-44" aria-label="QR minh họa không thể thanh toán">{Array.from({ length: 49 }, (_, index) => <span key={index} className={cells.includes(index) ? 'bg-pine' : 'bg-paper-deep'} />)}<strong className="absolute inset-x-1 top-1/2 -translate-y-1/2 -rotate-6 bg-coral py-2 text-center text-[0.62rem] text-paper">KHÔNG QUÉT</strong></div><dl className="space-y-3 text-sm"><div><dt className="text-ink-soft">Ngân hàng</dt><dd className="m-0 font-extrabold">TICKETLY DEMO BANK</dd></div><div><dt className="text-ink-soft">Số tiền</dt><dd className="m-0 text-xl font-extrabold text-blue-deep">{formatTicketPrice(amount)}</dd></div><div><dt className="text-ink-soft">Nội dung</dt><dd className="m-0 break-all font-extrabold">{transferContent}</dd></div></dl></div><p className="mt-5 rounded-md bg-error-ring p-3 text-sm font-extrabold text-error">KHÔNG CHUYỂN TIỀN — đây chỉ là màn hình demo.</p><label className={`mt-4 flex min-h-12 gap-3 rounded-md border bg-surface p-3 text-sm ${error ? 'border-error' : 'border-line'}`}><input ref={ref} className="mt-1 h-5 w-5 accent-blue" type="checkbox" checked={acceptedTerms} aria-invalid={Boolean(error)} aria-describedby={error ? 'primary-terms-error' : undefined} onChange={(event) => onAcceptedTermsChange(event.target.checked)} /><span>Tôi hiểu đây là prototype và không có giao dịch thật.</span></label>{error && <p id="primary-terms-error" className="mt-2 text-sm font-bold text-error" role="alert">{error}</p>}</section>
})
