import type { AttendeeOrderStatus } from '../types/order'
const steps: Record<AttendeeOrderStatus, readonly string[]> = { completed: ['Đơn hàng đã được tạo', 'Thanh toán demo thành công', 'Credential demo đã phát hành'], failed: ['Đơn hàng đã được tạo', 'Thanh toán demo thất bại', 'Không phát hành vé'], expired: ['Lựa chọn vé đã được ghi nhận', 'Phiên demo hết hạn', 'Không phát hành vé'] }
export function OrderDetailTimeline({ status }: { status: AttendeeOrderStatus }) { return <section className="mt-7 rounded-lg bg-paper-deep p-5"><h2 className="m-0 text-xl font-extrabold">Tiến trình đơn hàng</h2><ol className="mt-4 grid grid-cols-3 gap-3 p-0 mobile:grid-cols-1">{steps[status].map((step, index) => <li key={step} className="border-l-4 border-blue pl-3 text-sm font-bold"><span className="block text-xs text-blue-deep">0{index + 1}</span>{step}</li>)}</ol></section> }
